@model Dominio.Agenda

@{
    ViewBag.Title = "Excluir";
    Layout = "~/Areas/Painel/Views/Shared/_Painel.cshtml";
}

<h2>Excluir</h2>

<h3>Você realmente deseja excluir este registro?</h3>
<fieldset>
    <legend>Agenda</legend>

    <div class="display-label">
         @Html.DisplayNameFor(model => model.DataAgendada)
    </div>
    <div class="display-field">
        @Html.DisplayFor(model => model.DataAgendada)
    </div>

    <div class="display-label">
         @Html.DisplayNameFor(model => model.OrigemDestino)
    </div>
    <div class="display-field">
        @Html.DisplayFor(model => model.OrigemDestino)
    </div>
</fieldset>
@using (Html.BeginForm()) {
    @Html.AntiForgeryToken()
    <p>
        <input type="submit" value="Excluir" /> |
        @Html.ActionLink("Voltar", "Index")
    </p>
}
